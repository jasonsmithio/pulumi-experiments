name: gcp-py-wordpress-cloudrun
runtime:
  name: python
  options:
    virtualenv: venv
description: Basic example using WordPress on Cloud Run with Cloud SQL, GCLB, and Cloud Storage
template:
  config:
    gcp:project:
      description: The Google Cloud project to deploy into
    gcp:zone:
      description: The Google Cloud zone
    db-name:
      description: Name and Username of Cloud SQL database